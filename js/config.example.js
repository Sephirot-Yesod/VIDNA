/**
 * Configuration file - COPY THIS TO config.js AND ADD YOUR KEYS
 * 
 * 1. Copy this file: cp config.example.js config.js
 * 2. Add your API keys below
 * 3. config.js is gitignored so your keys stay safe
 * 
 * For Vercel deployment, set these as environment variables.
 */

const CONFIG = {
    // OpenRouter API key for AI filter generation
    OPENROUTER_API_KEY: 'your-openrouter-api-key-here',
    
    // Supabase configuration for user database
    SUPABASE_URL: 'https://your-project.supabase.co',
    SUPABASE_ANON_KEY: 'your-supabase-anon-key-here'
};
